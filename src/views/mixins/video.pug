//- 데이터를 받는 부분 mixin mixinName(obj)
mixin video(video) 
     div
        h4
            //- 속성값에는 #{} 사용 불가
            a(href=`/videos/${video.id}`)=video.title 
        p=video.description
        ul
            each hashtag in video.hashtags
                li=hashtag
        small=video.createdAt
        hr 